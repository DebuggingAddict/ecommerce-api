name: Shopping Mall CI/CD

on:
  push:
    branches: [ "main" ] # main 브랜치에 push될 때 작동

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Build and Push Docker Image
        run: |
          echo "${{ secrets.DOCKER_HUB_TOKEN }}" | docker login -u "${{ secrets.DOCKER_HUB_USERNAME }}" --password-stdin
          docker build -t ${{ secrets.DOCKER_HUB_USERNAME }}/my-app .
          docker push ${{ secrets.DOCKER_HUB_USERNAME }}/my-app

      - name: Deploy to EC2
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ubuntu
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            # 1. 포트 및 컨테이너 정리
            sudo fuser -k 8080/tcp || true
            sudo fuser -k 6379/tcp || true
            sudo docker-compose down || true
            sudo docker rm -f redis_container spring_app || true
            
            # 2. 이미지 업데이트
            sudo docker pull ${{ secrets.DOCKER_HUB_USERNAME }}/my-app
            
            # 3. .env 파일 생성 (따옴표 위치 주의)
            echo "CLOUD_AWS_ACCESS_KEY=${{ secrets.CLOUD_AWS_ACCESS_KEY }}" > .env
            echo "CLOUD_AWS_SECRET_KEY=${{ secrets.CLOUD_AWS_SECRET_KEY }}" >> .env
            echo "DB_URL=${{ secrets.DB_URL }}" >> .env
            echo "DB_USERNAME=${{ secrets.DB_USERNAME }}" >> .env
            echo "DB_PASSWORD=${{ secrets.DB_PASSWORD }}" >> .env
            echo "TOKEN_SECRET_KEY=${{ secrets.TOKEN_SECRET_KEY }}" >> .env
            echo "GOOGLE_CLIENT_ID=${{ secrets.GOOGLE_CLIENT_ID }}" >> .env
            echo "GOOGLE_CLIENT_SECRET=${{ secrets.GOOGLE_CLIENT_SECRET }}" >> .env
            echo "REDIS_HOST=redis" >> .env
            echo "REDIS_PORT=6379" >> .env
            echo "OAUTH2_REDIRECT_URL=${{ secrets.OAUTH2_REDIRECT_URL }}" >> .env
            echo "OAUTH2_FAILURE_REDIRECT_URL=${{ secrets.OAUTH2_FAILURE_REDIRECT_URL }}" >> .env
            echo "S3_IMAGE_BUCKET=${{ secrets.S3_IMAGE_BUCKET }}" >> .env
            
            # 4. docker-compose.yml 생성
            cat <<EOF > docker-compose.yml
            version: '3.8'
            services:
              redis:
                image: redis:alpine
                container_name: redis_container
                ports:
                  - "6379:6379"
              my-app:
                image: ${{ secrets.DOCKER_HUB_USERNAME }}/my-app
                container_name: spring_app
                ports:
                  - "8080:8080"
                depends_on:
                  - redis
                env_file:
                  - .env
                restart: always
            EOF
            
            # 5. 컨테이너 실행
            sudo docker-compose up -d