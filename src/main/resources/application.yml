server:
  port: 8080
  servlet:
    encoding:
      charset: UTF-8 # 한글 인코딩
      enabled: true # 인코딩 활성화
      force: true  # 요청/응답 모두 UTF-8 강제 적용 (한글 깨짐 방지)

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/shoppingmall_db?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8&allowPublicKeyRetrieval=true
    username: dev
    password: dev1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    defer-datasource-initialization: true  # ✅ Hibernate가 테이블 만든 후 data.sql 실행
    hibernate:
      ddl-auto: create
      #ddl-auto: update # 테이블 없으면 생성, 있으면 수정
      #ddl-auto: validate # 엔티티와 DB스키마의 일치 여부만 검증
    show-sql: true
    properties:
      hibernate.format_sql: true
      use_sql_comments: true
      dialect: org.hibernate.dialect.MySQL8Dialect
  sql:
    init:
      mode: always        # 🚀 data.sql 실행 활성화
      encoding: UTF-8
      continue-on-error: false  # ✅ 에러 발생 시 즉시 중단 (디버깅용)

logging:
  level:
    org.springframework.jdbc.datasource.init: DEBUG  # 🔍 data.sql 실행 로그
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE

token:
  secret:
    key: SpringSecurityKey_P_ssword_http__Spring_io
  access-token:
    plus-hour: 1
  refresh-token:
    plus-hour: 12